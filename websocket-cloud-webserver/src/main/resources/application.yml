server:
  port: 8071

spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: websocket-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  redis:
    host: localhost
    port: 6379
